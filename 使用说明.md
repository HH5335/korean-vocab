# BluePink 한글 使用说明

韩语背单词网站（多人版）。注册账号后，每个人的学习进度、错词本、打卡统计都是独立保存的。

---

## 一、启动网站（一键）

**双击 `启动网站.vbs`** —— 网站 + 公网隧道一起后台启动，无窗口。

- 本机访问：浏览器打开 `http://localhost:3001`
- 公网访问：打开 `tunnel.log.<当天日期>`（如 `tunnel.log.20260827`），文件**末尾**搜 `Tunnel established`，后面的 `https://xxx.cpolar.top` 就是公网网址（注意：`tunnel.log` 本身是空的，日志按日期滚动了）
- 停止：双击 `停止网站.bat`
- 启动后一直运行，关闭对话/终端也不影响；网站日志在 `runtime.log`
- 修改代码后重新上线：先停旧进程（`停止网站.bat`）→ `npm run build` → 再双击 `启动网站.vbs`

也可以分开启动：
- `start-production.vbs`：只启动网站（本机用）
- `start-tunnel.bat`：前台窗口启动隧道，能直接看到公网网址（Ctrl+C 停止）
- `start-tunnel.vbs`：后台启动隧道，网址写进 `tunnel.log.<当天日期>` 文件末尾

## 二、注册与登录

1. 首次打开自动跳到登录页
2. 点「去注册」，填用户名（1-20 位中英文/数字）和密码（至少 6 位）
3. 注册即登录，之后 30 天内免登录；右上角显示你的用户名，可随时「退出登录」

## 二点五、管理后台（仅管理员）

管理员登录后导航栏会多一个「🛠️ 管理」入口，页面显示：注册用户列表、最近登录记录、近 14 天每日活跃人数。普通用户看不到入口，直接访问 /admin 也会被拦回。

- 管理员账号是独立的 `admin` 账号（日常学习请用自己的账号）
- 新建/重置管理员：在 `server` 目录执行
  ```
  npx tsx scripts/create-admin.ts admin <新密码>
  ```
  （账号已存在时会直接设为管理员并重置密码）

## 三、局域网内访问（同一 WiFi）

浏览器打开 `http://<这台电脑的IP>:3001`，例如 `http://192.168.1.5:3001`。

- 查看本机 IP：`ipconfig` 里的「IPv4 地址」
- 若手机访问不了，需在 Windows 防火墙放行 3001 端口（管理员 PowerShell）：
  ```
  netsh advfirewall firewall add rule name="BluePink 3001" dir=in action=allow protocol=TCP localport=3001
  ```

## 四、公网访问（cpolar 内网穿透）

> 适合给不在同一网络的朋友使用。cpolar 客户端已随项目装在 `tools\cpolar\cpolar.exe`，账号令牌已配置好，直接启动即可。免费版网址随机（每次重启隧道可能变化）、有流量和速度限制，个人学习够用。

1. **双击 `启动网站.vbs`**（或只开隧道就双击 `start-tunnel.vbs`）
2. 打开 `tunnel.log.<当天日期>`（如 `tunnel.log.20260827`），拉到文件**末尾**搜 `Tunnel established` 找到 `https://xxx.cpolar.top` 公网地址，发给朋友即可（`tunnel.log` 本身是空文件，别开错了）
3. 注意：
   - 电脑关机/睡眠后网站和隧道都停，需重新双击 `启动网站.vbs`（网址可能变化）
   - cpolar 只是转发通道，网站本体始终跑在你电脑的 3001 端口
   - 换电脑/换账号：重新注册 cpolar 拿新令牌，执行 `tools\cpolar\cpolar.exe authtoken <新令牌>`

## 五、PDF 韩语书词表流水线

把韩语教材 PDF 里的单词提取出来，和现有词库（延世/TOPIK）合并成 Excel，并导入网站成为新词书。

### 目录说明

| 路径 | 作用 |
|---|---|
| `data\pdf-books\` | **把 PDF 放这里**（可多本，以后加新书直接丢进来） |
| `data\pdf-books\text\` | 每本 PDF 提取的原始文本（机器产物） |
| `data\pdf-books\extracted\` | **每本书的词表 CSV（人工订正工作区）** + 合并中间产物 |
| `data\韩语词表汇总.xlsx` | 最终 Excel 交付物 |

### 操作步骤（在 `scripts\pipeline` 目录执行）

1. 把 PDF 放进 `data\pdf-books\`，运行 `.venv\Scripts\python extract_pdf.py`（看每本字符统计；若警告"疑似扫描版"说明 PDF 没有文字层，需要 OCR，联系 Claude 另议）
2. 运行 `.venv\Scripts\python parse_pdf.py` 生成 `extracted\<书名>.csv`，**用 Excel 打开人工订正**：
   - 直接改 `hangul / pos / meaning_cn / example_ko / example_zh` 各列
   - `status` 列填 `delete` = 这行作废；填 `new` = 手工新增的行；留空 = 正常
   - `note` 列随便写备注
   - ⚠️ 保存时用「另存为 → CSV UTF-8」，否则中文会乱码
3. 运行 `.venv\Scripts\python merge_vocab.py` 合并（**同一韩语词以 PDF 书为准**；例句优先用视频资源里的，其次书中例句）
4. 运行 `.venv\Scripts\python build_excel.py` 生成 `data\韩语词表汇总.xlsx`（汇总 + 每本书一张 + AI待生成 + 来源冲突四个 sheet）
5. 待 AI 例句：`.venv\Scripts\python list_pending_examples.py` 列出清单交给 Claude 生成，写入 `extracted\ai-examples.json` 后重跑 3、4 步
6. 导入网站：在 `server` 目录执行 `npx tsx scripts/import-pdf-books.ts`（**改后端后先停旧进程再 build 重启**，见第一节）

以后订正了 CSV，重跑 3、4、6 步即可同步 Excel 和网站。新书格式和默认规则不匹配时，在 `data\pdf-books\config.json` 里按书名配置解析规则。

## 六、常见问题

- **端口被占用**：`netstat -ano | findstr :3001` 找到 PID 后在任务管理器结束该进程
- **忘记密码**：目前没有找回功能，直接注册一个新账号即可
- **数据备份**：学习数据都在 `server\prisma\dev.db` 这一个文件里，复制它即完成备份
- **网站打不开**：先看 `runtime.log` 有无报错；确认 `start-production.vbs` 是否已运行（任务管理器里有 node.exe 进程）
